<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".PlaygroundApplication"
        android:allowBackup="false"
        android:icon="@mipmap/playground_app_ic_launcher"
        android:label="@string/playground_app_name"
        android:roundIcon="@mipmap/playground_app_ic_launcher_round"
        android:theme="@style/PlaygroundAppTheme"
        tools:ignore="DataExtractionRules">

        <activity
            android:name=".PlaygroundActivity"
            android:exported="true"
            android:theme="@style/PlaygroundAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>